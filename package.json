{
  "name": "hnwatch-pwa",
  "version": "0.1.0",
  "description": "Lightweight Hacker News Tracker PWA with hourly notifications",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "lint": "biome check .",
    "format": "biome format --write .",
    "serve:public": "npx http-server ./public -p 8000 -c-1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@playwright/test": "^1.48.0",
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "http-server": "^14.1.1",
    "husky": "^9.1.7",
    "vite": "^7.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
