{
  "name": "hnwatch-pwa",
  "version": "0.1.0",
  "description": "Lightweight Hacker News Tracker PWA with hourly notifications",
  "type": "module",
  "scripts": {
    "dev": "web-dev-server",
    "build": "echo 'Static build - copy public/* to dist/'",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@biomejs/biome": "^2.3.0",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
